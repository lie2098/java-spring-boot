spring:
  application:
    name: cruddemo

  datasource:
    url: jdbc:postgresql://localhost:5432/spring_boot_demo
    username: spring_boot_demo
    password: springboot

  main:
    banner-mode: off

logging:
  level:
    root: warn
    org:
      hibernate:
        SQL: trace
        orm:
          jdbc:
            bind: trace